# The scheduled GitHub Actions collector job
# (.github/workflows/collector.yml).
#
# This file no longer adds anything: telethon moved into requirements.txt
# when live collection (app/live.py) put a Telegram connection inside the
# web process too. It is kept rather than deleted because collector.yml
# installs from it by name, and because the two environments are still
# free to diverge — a collector-only dependency belongs here, not in the
# file that ships to the 512MB web tier.
-r requirements.txt
