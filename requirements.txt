# Render free web service — kept deliberately lean (512MB RAM tier).
fastapi>=0.115,<1.0
# Declared directly, not left to FastAPI's transitive pin: the routers use
# starlette's HTTP_422_UNPROCESSABLE_CONTENT, which does not exist before
# starlette 0.48 (verified — 0.47 raises AttributeError). FastAPI caps
# starlette's upper bound, so this floor is what actually forces a FastAPI
# new enough to run this code; the fastapi floor above alone resolved to a
# combination that imports but fails at request time.
starlette>=0.48,<2.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
email-validator>=2.2,<3.0
jinja2>=3.1,<4.0
python-multipart>=0.0.12
bcrypt>=4.2,<5.0
cryptography>=50.0,<51.0
aiogram>=3.16,<4.0
httpx>=0.27,<1.0
alembic>=1.14,<2.0
# Optional TOTP second factor (idea 79). Pure Python, 45KB, and no runtime
# dependencies of its own — verified, because this ships to the 512MB web
# service. No external service and no subscription: the codes are computed
# locally from a shared secret, which is what keeps the second factor
# inside the zero-cost constraint.
pyotp>=2.9,<3.0
# Live collection (app/live.py) holds an open Telegram connection inside
# this process, so telethon now ships here as well as to the scheduled
# collector. It used to be excluded from this file to keep the 512MB tier
# lean, and that reasoning still deserves an answer: telethon is pure
# Python and pulls only pyaes and rsa, and app/live.py imports it lazily
# behind a try/except, so a deployment with LIVE_COLLECTOR_ENABLED unset
# pays an import that never runs. What it buys is a link stored in about a
# second instead of up to an hour.
telethon>=1.36,<2.0
