# ============================================================
# Link Intelligence Platform — Python Dependencies
# Tested on Python 3.11+
# ============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0
starlette==0.41.3

# --- Telegram ---
telethon==1.37.0
cryptg==0.4.0
# Bot API alternative. 3.15.5 is unavailable on PyPI; the 3.15.x line
# is API-stable, so pin the range instead of the exact patch.
aiogram>=3.15.0,<3.16

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
# sqlite-vec for semantic vector search on SQLite (no Docker needed)
sqlite-vec==0.1.6
# FTS5 ships with sqlite3 by default in modern Python

# --- Redis ---
redis==5.2.1
rq==2.0.0

# --- LLM providers ---
httpx==0.28.1
openai==1.58.1  # used for OpenRouter + Groq (OpenAI-compatible)
google-generativeai==0.8.4
huggingface_hub==0.26.5

# --- Crypto & security ---
cryptography==44.0.0
passlib[bcrypt]==1.7.4
pyjwt==2.10.1

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- Utilities ---
# (httpx already pinned above under LLM providers — do not repeat here.)
tenacity==9.0.0
python-dateutil==2.9.0
loguru==0.7.3
rich==13.9.4
orjson==3.10.12
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Async ---
anyio==4.7.0
nest-asyncio==1.6.0

# --- Embeddings (local, free) ---
sentence-transformers==3.3.1

# --- v4.1 Prometheus metrics ---
prometheus-client==0.21.1

# --- v4.1 WebSocket support (bundled with FastAPI/uvicorn[standard]) ---
websockets==13.1

# --- v5.0 Stripe billing ---
stripe==11.4.1

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
respx==0.22.0

# --- Linting & formatting (dev) ---
ruff==0.8.4
mypy==1.14.0
