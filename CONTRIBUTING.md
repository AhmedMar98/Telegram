# المساهمة

الفكرة ٢٣٣. الشرط الوحيد غير القابل للتفاوض هنا ليس أسلوب الشيفرة بل هذا:

> **لا ادّعاء في شيفرة أو وثيقة أو رسالة commit لم يُتحقَّق منه مقابل
> المستودع نفسه.**

كل ما تحت هذا العنوان تفصيلٌ لتلك الجملة.

---

## القيود التي تُردّ عندها المساهمة فوراً

ليست تفضيلات. مساهمة تخالف أيّاً منها تُرفَض مهما كانت جيّدة:

| القيد | لماذا |
|---|---|
| **بلا Docker** | قرار صريح لصاحب المشروع |
| **بلا Oracle Cloud** | القرار نفسه |
| **تكلفة صفرية غير مشروطة** | لا خطة مدفوعة، ولا واجهة مقيسة خارج طبقة مجانية **دائمة**. «مجاني لأوّل ١٠٠٠ طلب» ليس مجانياً |
| **بلا عملية خلفية دائمة** | لا خطة عامل مجانية على Render. كل ما يحتاج دورية = تشغيلة مجدولة |
| **بيانات الاعتماد لا تُسجَّل ولا تُعاد** | جلسات تلغرام، رموز البوت، مفاتيح API، عناوين الـwebhook |

---

## الدورة — سبع خطوات، لا واحدة اختيارية

مأخوذة من `docs/06-execution-plan.md` §٦، وهي الدورة التي مرّت بها كل دفعة
في تاريخ هذا المشروع:

1. **تحقّق من البوّابات** — هل الفكرة ممكنة ضمن القيود أعلاه؟
2. **تصميم مكتوب** إن كانت كبيرة (L أو XL).
3. **تنفيذ + اختبار يفشل قبل التغيير وينجح بعده.**
4. **بوّابة جودة كاملة** (أدناه).
5. **تحقّق حيّ** على خادم حقيقي حيثما كان الأثر ملموساً.
6. **تحديث الوثائق المتأثّرة بالأرقام الفعلية بعد التغيير، لا قبله.**
7. **PR، مراقبة CI، دمج.**

---

## بوّابة الجودة

```bash
python -m ruff check .
python -m ruff format --check .
python -m mypy --python-executable "$(which python3)" app scripts
python -m bandit -r app scripts
python -m pytest -q
```

**مزلقان كلّفا وقتاً فعلياً في هذا المستودع:**

- **`mypy` بلا `--python-executable`** يحلّ `aiogram` إلى `Any` فيمرّ نظيفاً
  محلياً ويفشل في CI.
- **`ruff check --fix` قد يطبع «No fixes available (1 hidden fix…)»**، وهي
  **رسالة خطأ** لا نجاح. لذلك تنتهي البوّابة بـ`ruff check .` مجرّداً.

**وPostgres ليس اختيارياً** إن لمست الشيفرة بحثاً أو هجرة:

```bash
DATABASE_URL="postgresql://…/app" PG_TEST_DSN="postgresql://…/search" python -m pytest -q
```

قاعدتان مختلفتان. `conftest.py` يرفض تشغيلهما على واحدة، ويشرح السبب.

---

## الاختبارات

**الاختبار الذي لا يفشل حين تُكسَر الميزة ليس اختباراً.** الطريقة المتّبعة
هنا: اكسر الشيفرة عمداً، وتأكّد أنّ الاختبار يسقط، ثم أصلحها. عدّة أخطاء
حقيقية في هذا المستودع اكتُشفت هكذا.

وثمّة صنف ثانٍ يُسمّى هنا **«حارس الحارس»**: اختبار يفشل حين يُضاف شيء
جديد بلا تغطية.

| الحارس | يمنع |
|---|---|
| `tests/test_isolation_sweep.py` | نقطة جديدة تأخذ معرّفاً بلا اختبار عزل |
| `tests/test_auth_boundary.py` | نقطة جديدة يصلها مفتاح API وما كان ينبغي |
| `tests/test_docs.py` | وثيقة تشير إلى ملفّ غير موجود، أو مولَّدة قديمة |
| `test_every_declared_alert_type_has_something_that_raises_it` | نوع تنبيه بلا ما يُطلقه |

**إن أضفت نقطة نهاية، سيفشل حارسان قبل أن تصل إلى CI.** ذلك مقصود: اذهب
وسجّلها فيهما عن قصد.

---

## الهجرات

- رقم متسلسل: `NNNN_وصف_قصير.py`.
- **`upgrade` و`downgrade` كلاهما**، ومُختبَران فعلاً ذهاباً وإياباً.
- **لا تستورد شيفرة التطبيق داخل هجرة.** الهجرة تصف لحظةً في الماضي؛ لو
  استوردت مساعِداً حيّاً لتغيّر ما فعلته هجرةٌ **طُبّقت سلفاً** حين يتغيّر
  ذلك المساعد. انسخ ما تحتاجه إليها وجمّده. `0018_feedback_domain.py`
  مثالٌ مشروح.
- بعدها: `alembic upgrade head && alembic check` نظيفاً على المحرّكين.
- وإن تغيّر المخطّط: `python scripts/db_report.py --schema`.

---

## الوثائق المولَّدة — لا تُحرَّر يدوياً

| الوثيقة | المولِّد |
|---|---|
| `docs/12-schema.md` | `python scripts/db_report.py --schema` |
| `docs/28-api-examples.md` | `python scripts/api_examples.py` |
| `docs/29-env-vars.md` | `python scripts/env_report.py` |

لكلٍّ اختبار قِدَم يفشل إن نسيت.

---

## رسائل الـcommit

طويلة عمداً في هذا المستودع. الشكل المتّبع: **ما القرار، وما البديل، ولماذا
هذا، وماذا كلّف.** والأهمّ: **الأرقام مقيسة لا مقدَّرة.** إن قلت «أسرع»
فقل بكم ومقابل ماذا.

وإن اكتشفت خطأً في عملك أثناء العمل، **اذكره**. عدّة رسائل هنا تحتوي قسم
«تصحيح داخل هذه الدفعة»، وهي أفضل ما فيها.

---

## ما لا يُقبَل

- تحسين أداء بلا قياس مسبق يثبت وجود مشكلة. المرحلة ١١ كاملة **مؤجَّلة
  رسمياً** لهذا السبب: القياس أظهر بحثاً في ٦٠–٧٥ ملّي ثانية، فلا مشكلة.
- ادّعاء امتثال قانوني. الفارق بين «ميزة تقنية موجودة» و«امتثال» مبدأ ثابت.
- إعادة اقتراح فكرة مرفوضة بلا سياق جديد — راجع `docs/06-rejected.md` أوّلاً؛
  وُجد لهذا بالضبط.
- تبعية جديدة بلا مبرّر يذكر حجمها. الخدمة على طبقة ٥١٢ ميغابايت.
